[main]
profile=default
input_directory=.
output_directory=.
video_extensions=mkv,mp4
poll_frequency=60
loglevel=info

[profile|default]
encoder=ffmpeg.exe
parameters=-i ${input} -c:v libx264 -crf 22 -preset:v fast -ac 2 -c:a aac -b:a 160k -vf subtitles=${input} ${output}